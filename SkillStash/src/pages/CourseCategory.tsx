const CourseCategory = () => {
  return (
    <main className="p-8 bg-background">
      <h1 className="text-4xl font-bold">Course Categories</h1>
      <p className="mt-4 text-lg text-gray-600">
        Browse courses by category. (e.g., Development, Business, Design, etc.)
      </p>
      {/* We will add category links or cards here later */}
    </main>
  );
};

export default CourseCategory;